---
# DECISIONS.md — Autonomous Redesign Build
All forks, taste calls, and deviations logged here. Append; never overwrite.

| # | Decision | Rationale |
|---|----------|-----------|
| 1 | Kept legacy `--atlas-*` CSS variables as aliases to new light tokens | Existing components use `var(--atlas-bg)`, `var(--atlas-ink)` etc. Remapping them to the new light values avoids a mass search-and-replace across all components while maintaining correct rendered output under the new token system. |
| 2 | `ReefHealthBadge` uses `improving/stable/declining` state names; `ReefStateBadge` keeps `thriving/pressure/change` | The data layer uses `thriving/pressure/change`. The new badge is for display purposes and uses the design spec names. The old badge re-exports the new one alongside itself so both work without touching data files. |
| 3 | Logo updated to text-only wordmark (removed SVG water-drop icon) | Design system rule: no icons in UI (UX-DR20). Logo is now Source Serif 4 "Scuba Season" text. `dark` prop kept for API compatibility. |
| 4 | AtlasNav `HeroNav` variant removed | New nav handles transparent/solid state via scroll detection on a single component. The hero variant was only used on pages that are now on the Epic 2 backlog. Removing it reduces duplicate code and avoids the old `hideLayoutNav` complexity for the nav. |
| 5 | Footer hover colors implemented via `<style>` block CSS (not inline `onMouseEnter`) | AtlasFooter is a Server Component. Event handlers cannot be passed in RSC. CSS `:hover` achieves the same hover-to-yellow effect without requiring `"use client"`. |
| 6 | Fixed pre-existing bug in `src/lib/atlas-location.ts` line 177 | 358/380 sites have no `species` field. `s.species.map(...)` crashed with "Cannot read properties of undefined". Added `?? []` null-coalesce guard. Not a data write — guard is in application code only. |
| 7 | Nav search input uses `rounded-sm` (2px radius) not pill shape | Pill search was part of old Kimi rebrand aesthetic. New design uses rectangular elements (border-radius 2px per spec). |
| 8 | `/explore` route not yet created (Epic 3); nav "Explore" link points to `/locations` | Spec says `/explore` but that route is Epic 3 scope. Using `/locations` as fallback so nav is functional on Epic 1 delivery. |
| 9 | Hero photo uses OceanImageBank_FilippoBorghi_04.jpg (Raja Ampat manta ray) | No local underwater photo asset exists in `/public`. This is the most dramatic underwater shot already referenced in the existing homepage code, confirmed underwater via OceanImageBank source. DECISIONS.md notes this per Epic 2 Story 2.1 requirement. |
| 10 | ReefStateCard trio uses computed reef states (improving/stable/declining) not the data layer's thriving/pressure/change | The data layer returns `thriving/pressure/change` but the design spec and ReefHealthBadge use `improving/stable/declining`. Cards are hardcoded server-side with the mapped display state matching the underlying data computation result. No data files were modified. |
| 11 | HomepageStatStrip is a new Client Component (`homepage-stat-strip.tsx`) separate from existing `stat-strip.tsx` | The existing StatStrip has a different API and styling. Epic 2 spec requires yellow border-y, counter animation, and a different layout. Creating a new component avoids breaking existing stat-strip usages in location/site pages. |
| 12 | SpeciesFilmstrip uses 10 hardcoded species with iNaturalist photo thumbnails | No `src/data/species.json` exists. The species data lives inside each site's `species` array within `sites.json`. Building per-species sighting odds cross-site would require a data aggregation step that is out of scope (data read-only rule). Hardcoded sample data fulfills Story 2.5 display requirements with real iNat photo URLs. |
| 13 | `/submit` route is the Upload CTA destination | The sighting submission feature exists in `src/app/api/` and `src/components/sighting-submission/` (untracked). CTAs point to `/submit` as the presumed submission route. If the route slug differs, CTAs need updating when Epic 5 lands. |
| 14 | Hero bottom gradient fades to white (#FFFFFF not transparent) | The spec says "from-transparent to-white" for the bottom gradient. This creates a clean transition into the yellow-bordered StatStrip. The gradient goes to exact `--color-paper: #FFFFFF`. |
| 15 | Epic 3: new `ExplorePage` component replaces `LocationsExplorer` entirely | The existing `LocationsExplorer` was dark-themed (#0a1628 bg), used the old cyan (#00d4ff) accent, and had no map+grid split layout. Rather than patch it in-place (which would require extensive override), a new `explore-page.tsx` was created implementing all 5 stories with the new light tokens. The old `LocationsExplorer` is kept because it may be referenced elsewhere; the page now imports `ExplorePage`. |
| 16 | Epic 3: `FilterBar` simplified to 3 filter categories (reef state, broad region, month) | The spec asks for reef state + region + best season month pills. The existing explorer had 6+ filter types (cert, "what to see", fresh eyes, etc.). The simplified `FilterBar` is the primary pill bar; the mobile sheet adds the same 3 categories. The removed filter types (cert, wildlife, fresh eyes) are accessible on the old component if needed but out of scope for Epic 3 stories 3.1–3.5. |
| 17 | Epic 3: `hasSightings` computed by checking `siteIds` from base locations.json → `getSightingsBySiteId` | The sightings data is indexed by `siteId`, not locationId. Base `locations.json` has a `siteIds` array. The page computes `hasSightings` server-side by checking if any site of the location has at least 1 sighting record. No data files modified. |
| 18 | Epic 3: `LocationsGlobe` updated to accept optional `onMarkerClick` prop | Previously, all globe marker clicks navigated to `/locations/[slug]`. The explore page needs marker clicks to select and scroll to the corresponding card (not navigate away). Added optional `onMarkerClick?: (slug: string) => void` — when provided it overrides the default navigate behavior. Globe height also made configurable. Backwards-compatible change. |
| 19 | Epic 3: Hover border on `ReefCard` uses global `.reef-card:hover` CSS from globals.css | The existing globals.css already defines `.reef-card:hover { border-color: var(--color-ink) }`. The `ReefCard` component adds `className="reef-card"` to pick up this rule instead of injecting per-card `<style>` tags, which would cause a flash of unstyled border on hydration. |
| 20 | Epic 4: `coral-projection-chart.tsx`, `sighting-log.tsx`, `fishing-pressure-panel.tsx` already existed — not re-created | All three were pre-implemented components from a prior session. Read them before writing and confirmed they already match the story spec. Only `site-page-body.tsx` and wiring in page.tsx files needed to be written/updated. |
| 21 | Epic 4: Story 4.2 `projectionDataPoints` built from historicalCoralCoverPercent + coralCoverPercent pair | The data schema has exactly two coral cover data points per reef-health record (historical baseline + current survey). These map directly to the 2-point CoralDataPoint array. Locations with only 1 or 0 points render nothing (chart guards at < 2 points). |
| 22 | Epic 4: Story 4.3 `fishingPressureData` is null — no GFW hour counts in ReefPressureRecord | `ReefPressureRecord` contains `fishingPressure` (level string: low/moderate/high) not raw GFW fishing hours. The fishing pill (plain-language level) is shown instead. When GFW raw data is added to the schema, wiring can be added with no UI changes. |
| 23 | Epic 4: Story 4.4 wreck section omitted — no structured wreck object in sites.json | The spec says "render only when wreck data exists". sites.json identifies wreck sites via `diveTypes` array (e.g. "wreck") but has no structured `wreck: { vesselName, sunkDate, ... }` object. Rendering from `diveTypes` alone would require fabricating data. Omitted until a wreck schema is added to the data layer. |
| 24 | Epic 4: Story 4.6 SightingLog entries sourced from sightings.json species-occurrence aggregates | sightings.json is an aggregated species-occurrence dataset, not individual diver log entries. `SightingLog` receives entries derived from this with `diverName: null` (rendered as "Anonymous"). When live iNaturalist observations flow in via the Epic 5 ingest, each will be a real per-diver entry. |
| 25 | Epic 4: Fixed pre-existing null-safety bugs in location and site page.tsx | `sites.flatMap(s => s.operators)` and `s.siteSpecificGear` crashed on sites where these fields are null in the JSON. Added `?? []` guards. Also filtered operators/lodging arrays to exclude records missing the `partner` field (some sites have operators in legacy `{name, url, type}` schema instead of `{partner, label, url, isAffiliate}`). These are data quality issues, not Epic 4 changes. |
| 26 | Epic 5: sharp not installed — HEIC photos submitted as-is to iNaturalist | `sharp` is not in package.json. The spec says "if sharp not available, log in DECISIONS.md and skip conversion, submit as-is." HEIC files are accepted client-side (shown as filename label in preview, not broken img tag) and passed through to the API unchanged. iNaturalist accepts HEIC on the /observation_photos endpoint. If conversion becomes necessary, install sharp and add the conversion step in `maybeConvertHeic()` in route.ts. |
| 27 | Epic 5: iNaturalist sign-in shows "available August 2026" note | Per spec: clicking "Sign in with iNaturalist" on Step 3 shows an inline note explaining API registration is complete ~2026-08-15. The radio selects inat mode but the form still submits via the Scuba Season guest account. No auth flow is wired — this is a placeholder. |
| 28 | Epic 5: Site search is simple substring match, not fuzzy (fuse.js absent) | fuse.js is not in package.json. Used `String.prototype.includes` on site name and locationId (case-insensitive). Matches on both fields; up to 8 results shown. Sufficient for the ~380 site dataset. Add fuse.js if typo tolerance is needed. |
| 29 | Epic 5: exifr not installed — using manual DataView EXIF reader | exifr is not in package.json. Copied the lightweight DataView EXIF reader from the existing SubmissionForm (reads only date tag 0x9003/0x0132, first 128 KB of file). GPS fields are not read from EXIF; lat/lng come from the selected site record as per spec ("NOT from EXIF"). |
| 30 | Epic 5: Multi-species creates one iNat observation per matched taxon | Spec says "if multiple taxon_ids submitted, create one observation per taxon." The API route loops `submitToInat` for each taxonId. Free-text species with no taxonId are added to the description of a single observation with `needsReview: true`. The observationUrl returned is from the last observation created. |
| 31 | Epic 5: `/upload` route is the canonical standalone upload page | Previously CTAs pointed to `/submit` (Decision 13). The new standalone wizard lives at `/upload`. Updated route per spec. `/submit` route (if it exists) is not removed — the wizard is the new primary entry point. |
| 32 | Epic 6: Method page — dark tokens replaced with light design system CSS vars | Replaced all hardcoded dark hex values (#0b1e32, #0a1628, #f0f4f8, #aebcd0, #8b9db8, #00d4ff) with --color-* and --font-* CSS variables from globals.css. BRAND color (#00d4ff) replaced with --color-ocean (#0E4F6E) for links; yellow (#F6C700) used for CTAs only. |
| 33 | Epic 6: Method page — reef state card colors mapped to design token set | Thriving → --color-improving, Under pressure → --color-stable, Witnessing change → --color-declining. These are the only health colors used in the UI per global rules. |
| 34 | Epic 6: Method page — "IUCN Red List" heading kept but codes removed from user-facing UI | h2/h3 headings now say "Conservation labels" and "Conservation status" rather than embedding IUCN codes. IUCN codes remain only in structured data attributes and the IucnBadge component (unchanged). |
| 35 | Epic 6: About page — all prose preserved word-for-word, only styling changed | Per story 6.2: zero text changes. Switched dark header (0b1e32 bg, #f0f4f8 text) to white bg + --color-ink text. h1 converted to font-serif 300 italic. Roadmap pill colors moved from purple/teal to --color-improving / --color-ocean / --color-ink-2 to stay on token system. CTA button uses --color-brand-yellow. |
| 36 | Epic 6: About page — FTC affiliate disclosure added | The original about page had no disclosure block. Added a plain-text FTC disclosure paragraph between editorial principles and roadmap sections. Uses --color-hairline border-left treatment (not a box). |
| 37 | Epic 6: Species list page — breadcrumb now includes "All species" link back | Added explicit "All species" step in breadcrumb on the detail page so users can navigate back without using browser history. |
| 38 | Epic 6: Species detail page — h1 now uses font-serif 300 (common name only) | Scientific name demoted to italic subtext below h1. h1 only displays common name. IUCN codes remain in IucnBadge component (unchanged), which maps codes to plain English labels. Section headings use --font-mono eyebrow treatment (uppercase tracking) not h2 bold. |
| 39 | Epic 6: Species list page — IUCN tones remapped to light palette | Replaced rgba(255,255,255,...) alpha-on-dark tones with rgba(46,125,91,...) / rgba(192,65,43,...) / rgba(185,138,46,...) alpha-on-white. Text colors use --color-improving / --color-stable / --color-declining tokens. |
