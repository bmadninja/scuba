# Decision Log — scuba UX Design
# Run: ux-scuba-2026-06-03

---

## Session 1 — 2026-06-03

### D01 — Working mode: Live codebase
Source: user ("start fresh / based on what's actually live")
Decision: Both spines distilled directly from live codebase (src/, globals.css, components.json). No planning docs used.

### D02 — Sources
- src/app/**/*.tsx (all pages)
- src/components/**/*.tsx (all components)
- src/app/globals.css (design tokens)
- components.json (shadcn config)
- src/app/layout.tsx (fonts, theme)

### D03 — Key correction vs prior run
Prior run assumed dark-first. Live code is light-first: body = `bg-white text-slate-900`. Dark class defined in CSS but not applied by default layout.

### D04 — Under pressure color correction
Prior run used amber for "Under pressure." Live STATE_SWATCH in filter rail: pressure = "#0089de" (brand blue). Pill in ReefLocationCard uses `bg-[#eaf1fe] text-[#1f57c8]` (blue family). Documented correctly in this run.

### D05 — Status
DESIGN.md: distilled from live code. EXPERIENCE.md: distilled from live code. Both draft.
